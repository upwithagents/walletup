# Secrets — never commit
.env
.env.*

# OS / IDE
.DS_Store
.idea/

# PRIVACY: anything disclosing Wallet details (payers, payees, amounts,
# record IDs) or identifying info (real name, employer) must stay out of
# git — this repo is public at github.com/upwithagents/walletup
Finances - AI powered GSheet/Snapshot_*/
agent_reviews/
reports/
CLAUDE.local.md
apps/web/prisma/seed-data/*.json
!apps/web/prisma/seed-data/example.json
*.db
*.db-journal

# Node
node_modules/
.next/
next-env.d.ts

# Claude workspace dirs that stay local
claude_tasks/
claude_procedures/
claude_plans/
.claude/memory/
