{
  "name": "walletup",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "db:migrate": "pnpm --filter @walletup/db db:migrate",
    "db:seed": "pnpm --filter @walletup/db db:seed",
    "typecheck": "pnpm --filter @walletup/hub typecheck"
  },
  "packageManager": "pnpm@10.30.1",
  "devDependencies": {
    "playwright": "^1.61.1",
    "tsx": "^4.23.0"
  }
}
